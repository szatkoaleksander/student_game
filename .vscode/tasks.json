{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/student_game.backend/student_game.backend.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}